{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "compact": {
      "outputs": ["src/artifacts/**"]
    },
    "compact:fast": {
      "outputs": ["src/artifacts/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "test": {
      "dependsOn": ["build"]
    },
    "deploy": {
      "dependsOn": ["build"],
      "cache": false,
      "passThroughEnv": ["MIDNIGHT_SEED", "MIDNIGHT_RPC_URL", "MIDNIGHT_INDEXER_URL", "MIDNIGHT_PROOF_SERVER_URL"]
    },
    "deploy:set-rates": {
      "dependsOn": ["build"],
      "cache": false,
      "passThroughEnv": ["MIDNIGHT_SEED", "MIDNIGHT_RPC_URL", "MIDNIGHT_INDEXER_URL", "MIDNIGHT_PROOF_SERVER_URL"]
    }
  }
}
